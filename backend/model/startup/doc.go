// Package startup is for getting the application started.
// It handles the applications very first startup and each startup after that.
// It begins with startup.Start(ctx context.Context, ctxCancel context.CancelFunc)
package startup
